import 'package:flutter/material.dart';

class AnidexScreen extends StatelessWidget {
  const AnidexScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Anidex')),
      body: const Center(
        child: Text('Placeholder for Anidex'),
      ),
    );
  }
}